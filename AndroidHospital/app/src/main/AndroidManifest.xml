<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidHospital"
        tools:targetApi="31">
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AndroidHospital.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="MainActivity"
            android:theme="@style/Theme.AndroidHospital.NoActionBar">

        </activity>

        <activity
            android:name=".AddPatientActivity"
            android:exported="true"
            android:label="AddPatientActivity"
            android:theme="@style/Theme.AndroidHospital.NoActionBar">

        </activity>

        <activity
            android:name=".UpdateRecordActivity"
            android:exported="true"
            android:label="UpdateRecordActivity"
            android:theme="@style/Theme.AndroidHospital.NoActionBar">

        </activity>

        <activity
            android:name=".ReturnUserKey"
            android:label="ReturnUserKey"
            android:exported="true"
            android:theme="@style/Theme.AndroidHospital.NoActionBar">
        </activity>

        <activity
            android:name=".CheckReportActivity"
            android:label="CheckReportActivity"
            android:exported="true"
            android:theme="@style/Theme.AndroidHospital.NoActionBar">
        </activity>

        <activity
            android:name=".ShowReport"
            android:label="ShowReport"
            android:exported="true"
            android:theme="@style/Theme.AndroidHospital.NoActionBar">
        </activity>



    </application>

</manifest>